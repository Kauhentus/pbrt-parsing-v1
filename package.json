{
  "name": "pbrt-parser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "run": "node ./build/index.js",
    "dev": "npm run build && npm run run",
    "dev-all": "npm run grammar && npm run build && npm run run",
    "grammar": "nearleyc ./src/grammar.ne -o ./src/grammar.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/nearley": "^2.11.5",
    "@types/node": "^20.12.7"
  },
  "dependencies": {
    "mathjs": "^12.4.2",
    "ml-matrix": "^6.11.0",
    "moo": "^0.5.2",
    "nearley": "^2.20.1"
  }
}
